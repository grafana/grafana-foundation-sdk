import * as cog from '../cog';
import * as elasticsearch from '../elasticsearch';
export declare class RawDocumentBuilder implements cog.Builder<elasticsearch.RawDocument> {
    private readonly internal;
    constructor();
    build(): elasticsearch.RawDocument;
    id(id: string): this;
    settings(settings: {
        size?: string;
    }): this;
    hide(hide: boolean): this;
}
