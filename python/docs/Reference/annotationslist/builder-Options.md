---
title: <span class="badge builder"></span> Options
---
# <span class="badge builder"></span> Options

## Constructor

```python
Options()
```
## Methods

### <span class="badge object-method"></span> build

Builds the object.

```python
def build() -> annotationslist.Options
```

### <span class="badge object-method"></span> limit

```python
def limit(limit: int) -> typing.Self
```

### <span class="badge object-method"></span> navigate_after

```python
def navigate_after(navigate_after: str) -> typing.Self
```

### <span class="badge object-method"></span> navigate_before

```python
def navigate_before(navigate_before: str) -> typing.Self
```

### <span class="badge object-method"></span> navigate_to_panel

```python
def navigate_to_panel(navigate_to_panel: bool) -> typing.Self
```

### <span class="badge object-method"></span> only_from_this_dashboard

```python
def only_from_this_dashboard(only_from_this_dashboard: bool) -> typing.Self
```

### <span class="badge object-method"></span> only_in_time_range

```python
def only_in_time_range(only_in_time_range: bool) -> typing.Self
```

### <span class="badge object-method"></span> show_tags

```python
def show_tags(show_tags: bool) -> typing.Self
```

### <span class="badge object-method"></span> show_time

```python
def show_time(show_time: bool) -> typing.Self
```

### <span class="badge object-method"></span> show_user

```python
def show_user(show_user: bool) -> typing.Self
```

### <span class="badge object-method"></span> tags

```python
def tags(tags: list[str]) -> typing.Self
```

## See also

 * <span class="badge object-type-class"></span> [Options](./object-Options.md)
